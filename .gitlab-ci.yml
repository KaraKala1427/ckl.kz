stages:
  # - vendor
  - upload

image: registry-backend.cic.kz/centras/ckl:latest

before_script:
  - echo composer install --no-scripts --no-autoloader
  - echo composer dump-autoload --optimize

# vendor:
#   stage: vendor
#   tags:
#     - ckl
#   image: composer
#   script: 
#     - composer install --ignore-platform-reqs

upload:
  stage: upload
  tags:
    - ckl
  script:
    # Sync to FTP
    - lftp -e "open $FTP_HOST; user $FTP_USERNAME $FTP_PASSWORD; mirror -X .* -X .*/ --reverse --verbose --delete ./ www/test.ckl.kz/ --exclude public; bye"
