image: ubuntu:20.04

before_script:
  - apt-get update -qy
  - apt-get install -y lftp

build:
  tags:
    - ckl
  script:
    # Sync to FTP
    - cat /etc/lftp.conf
    - lftp -e "open $FTP_HOST; user $FTP_USERNAME $FTP_PASSWORD; mirror -X .* -X .*/ --reverse --verbose --delete ./ www/test.ckl.kz/; bye"